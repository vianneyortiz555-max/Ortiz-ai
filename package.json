{
  "name": "ortiz-ai",
  "version": "2.12.0",
  "private": true,
  "description": "Ortiz AI v2 - authenticated AI chat web app",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check netlify/functions/chat.js && node --check netlify/functions/stripe-webhook.js && node --check server.js && node --check app.js"
  }
}
